from Key import Key
from Safe import Safe

def main():

    file = open("codes/keys.list")
    keys = file.readlines()

    # TODO: Createa a Safe object

    # TODO: Iterate over the keys to check each
    #      possible key against the safe's
    #      crack method.
    #
    #      If the result is True, break

    if # TODO test is_cracked property of Safe object
        with open("bonanza", "w") as output:
            output.write(f"{safe}")


if __name__=="__main__":
    main()
